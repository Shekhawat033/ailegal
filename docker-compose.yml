services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ailegal
      POSTGRES_PASSWORD: ailegal
      POSTGRES_DB: ailegal
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ailegal -d ailegal"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
